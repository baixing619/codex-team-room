# Privacy

Codex Team Room 采用本地优先设计。

## 会读取什么

- 本机 Codex 会话索引中的任务 ID、标题、更新时间和工作目录。
- 用户主动点开的单条历史任务中的可见用户消息与助手回复。
- 用户主动发送到房间的文字、所选附件、当前项目知识副本和近期团队消息。

## 不会读取或发布什么

- `auth.json`、API Key、Cookie、浏览器档案或账号凭据。
- 未被用户点开的历史对话正文。
- 未被当前成员任务按权限读取的项目文件内容。
- Codex 的隐藏系统提示、开发者提示、权限说明和应用内部上下文。

## 本地存储

房间、成员、提示词、知识条目、近期群聊、项目路径映射和成员对话绑定先保存在当前浏览器的本地存储中；完成私人站点配对后，它们也会保存到所有者私人 D1，作为手机与电脑浏览器的同步源。配置导出仍排除聊天消息和命令记录。私人托管版本不能直接访问本机会话：配对电脑主动领取经过认证的同步、索引和任务请求，不开放入站端口。

手机上传的附件暂存在所有者私人 Sites 的 R2 存储中，配对电脑领取后写入操作系统临时目录，并在远程任务完成/失败回执后从 R2 删除。附件不会提交到 Git，也不会成为项目文件，除非用户明确要求 Codex 将内容写入项目。

启用真实成员运行时后，用户发送给发言成员的消息、共享上下文和附件会交给用户自己的 Codex CLI 及其当前配置的模型服务处理。Team Room 不代理、保存或共享用户凭据；实际服务数据处理仍受用户所使用的 Codex/OpenAI 账户与服务条款约束。

## 开源仓库

发布检查会拒绝认证文件、环境文件、会话目录、JSONL 会话记录、私钥模式和用户目录绝对路径。维护者仍应在提交前人工检查变更。
