# Security Policy

## 支持范围

安全修复只针对最新版本。发现漏洞时，请先私下联系维护者，不要在公开 issue 中粘贴令牌、聊天记录、项目源码或本机路径。

## 执行模型

当前 `0.1.0` 默认使用不执行真实命令的模拟模式。只有检测到独立 Codex CLI 且用户在设置页明确确认后，才会启动 App Server。真实模式遵循以下不可绕过的安全边界：

- 只读操作与写入操作分离。
- 写入和外部副作用必须由用户明确审批。
- 同一项目同一时间只有一个写入者。
- 审批必须绑定成员、命令、目标和有效期。
- 日志不得记录密钥或完整认证载荷。
- 真实写入审批只允许一次性 `accept`、`decline` 或 `cancel`，客户端拒绝会话级永久授权。

本地开发桥接默认只绑定回环地址 `127.0.0.1`，避免同一局域网设备读取项目与对话索引。静态部署不包含本地桥接端点。

## 威胁模型

本项目不将本地网页视为可信输入。历史消息可能包含恶意提示，因此历史对话只能作为引用内容，不能自动获得命令权限。
