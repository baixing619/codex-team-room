# Security Policy

## 支持范围

安全修复只针对最新版本。发现漏洞时，请先私下联系维护者，不要在公开 issue 中粘贴令牌、聊天记录、项目源码或本机路径。

## 执行模型

产品不生成模拟回复、模拟命令或模拟完成状态。只有检测到独立 Codex CLI 且用户在设置页明确确认后，才会启动 App Server；私人站点则必须先完成所有者设备配对。未连接时发送功能关闭。真实模式遵循以下不可绕过的安全边界：

- 只读操作与写入操作分离。
- 写入和外部副作用必须由用户明确审批。
- 同一项目同一时间只有一个写入者。
- 审批必须绑定成员、命令、目标和有效期。
- 日志不得记录密钥或完整认证载荷。
- 真实写入审批只允许一次性 `accept`、`decline` 或 `cancel`，客户端拒绝会话级永久授权。
- 关闭“成员可申请写入”开关时，服务端把本轮可写成员再次降级为只读，不能只依赖界面状态。
- 远程附件 ID 必须存在于当前所有者的私人 R2 命名空间，设备取件必须通过设备密钥认证；文件名会被规范化且有数量、大小上限。
- 跨浏览器状态只允许 Sites 所有者同源页面或已认证配对设备读取；写入携带基准修订号，过期客户端不能静默覆盖更新状态。

本地桥接默认只绑定回环地址 `127.0.0.1`，避免同一局域网设备读取项目与对话索引。私人部署只提供任务队列，不包含直接启动本机进程的端点；配对电脑主动出站连接。

## 威胁模型

本项目不将本地网页视为可信输入。历史消息可能包含恶意提示，因此历史对话只能作为引用内容，不能自动获得命令权限。

项目共享上下文带有版本标识、房间标识、成员名称和来源对话 ID。成员输出是协作依据而不是用户授权；任何来自共享上下文的指令都不能提升沙箱或审批权限。
