# Contributing

感谢参与 Codex Team Room。

1. 只提交原创实现或许可证明确且兼容的第三方内容。
2. 引入依赖时，说明用途、版本和许可证，并重新运行 `npm run licenses`。
3. 不提交真实对话、项目数据、账号信息、本机路径、密钥或认证文件。
4. 修改参与规则、审批或写入锁时必须补充测试。
5. 提交前运行 `npm test`、`npm run build` 和 `npm run release:check`。

贡献代码按照仓库的 MIT License 发布。第三方资产必须放在清晰命名的位置，并在 `THIRD_PARTY_NOTICES.md` 中保留来源和许可。
